package chatgm.com.mcp_server.config;

// Este arquivo foi removido pois não usamos mais email para autenticação.
// Apenas o token é aceito agora.

/**
 * @deprecated Este filtro foi removido pois o parâmetro 'email' foi completamente removido.
 */
// Esta classe foi intencionalmente deixada vazia e não será carregada pelo Spring.
